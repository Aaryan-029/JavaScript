const myObj = {
    'game1':'NFS',
    'game2':'Spiderman'
}

for (const key in myObj) {
    //console.log(`${key} shortcut is for ${myObj[key]}`);
    
      }

const arr = ['a','b','c']

for (const key in arr) {
    console.log(arr[key]);
    
}
